% rebase('layout.tpl', title='Алгоритм Дейкстры', year=year, active_page='dijkstra')

<h2>Алгоритм Дейкстры</h2>

<!-- ═══════════════════════════════════════════════════════════
     БЛОК 1 — Условие задачи
     ═══════════════════════════════════════════════════════════ -->
<div class="theory-section">
    <h3>Условие задачи</h3>
    <p>
        Компьютерная сеть задана взвешенным ориентированным графом,
        где вершины — сетевые узлы (маршрутизаторы), дуги — каналы связи,
        вес дуги — задержка передачи данных (в миллисекундах).
        Некоторые каналы могут быть временно недоступны (вес = ∞).
        Требуется для заданного узла-источника найти маршрут с минимальной
        суммарной задержкой до каждого узла назначения сети.
    </p>
    <p>
        Алгоритм: Дейкстра с модификацией для учёта метрики OSPF
        (недоступные каналы игнорируются).
    </p>
</div>

<!-- ═══════════════════════════════════════════════════════════
     БЛОК 2 — Ориентированный взвешенный граф
     ═══════════════════════════════════════════════════════════ -->
<div class="theory-section">
    <h3>Ориентированный взвешенный граф</h3>
    <p>
        Ориентированным графом называют пару G = (V, E), где E — множество
        упорядоченных пар (дуг). Дуга (u, v) означает: из узла u в узел v
        есть односторонний канал. В отличие от неориентированного графа,
        наличие дуги (u, v) не подразумевает наличие дуги (v, u).
    </p>
    <p>В сетевой модели OSPF (Open Shortest Path First):</p>
    <ul>
        <li>Каждый маршрутизатор знает топологию всей сети.</li>
        <li>Вес дуги — метрика: задержка, пропускная способность или стоимость.</li>
        <li>
            Дуга с весом ∞ означает временно недоступный канал (авария,
            перегрузка), такая дуга исключается из расчёта.
        </li>
        <li>
            Цель — найти путь с минимальной суммой весов от источника
            до каждого узла назначения.
        </li>
    </ul>
</div>

<!-- ═══════════════════════════════════════════════════════════
     БЛОК 3 — Алгоритм Дейкстры
     ═══════════════════════════════════════════════════════════ -->
<div class="theory-section">
    <h3>Алгоритм Дейкстры</h3>
    <p>
        Алгоритм Дейкстры находит кратчайшие пути от одной вершины-источника
        до всех остальных в графе с неотрицательными весами рёбер
        за время O((V + E) log V) с использованием приоритетной очереди.
    </p>
    <ol>
        <li>
            Инициализировать: <code>dist[s] = 0</code> для источника s,
            <code>dist[v] = ∞</code> для всех остальных вершин v.
            Добавить все вершины в очередь с приоритетом по dist.
        </li>
        <li>
            Извлечь из очереди вершину u с минимальным <code>dist[u]</code>.
        </li>
        <li>
            Для каждой дуги (u, v) с весом w:
            <ul>
                <li>если w = ∞ — пропустить (канал недоступен, модификация OSPF);</li>
                <li>
                    если <code>dist[u] + w &lt; dist[v]</code>:
                    обновить <code>dist[v] = dist[u] + w</code>,
                    запомнить <code>prev[v] = u</code> (для восстановления пути).
                </li>
            </ul>
        </li>
        <li>Повторять шаги 2–3 до опустошения очереди.</li>
        <li>
            Восстановить путь до любой вершины v: следовать по <code>prev[]</code>
            от v к s в обратном порядке.
        </li>
    </ol>
</div>

<!-- ═══════════════════════════════════════════════════════════
     БЛОК 4 — Модификация для OSPF
     ═══════════════════════════════════════════════════════════ -->
<div class="theory-section">
    <h3>Модификация для метрики OSPF</h3>
    <p>
        В протоколе OSPF маршрутизатор автоматически исключает
        недоступные каналы из расчёта маршрутов. Модификация алгоритма:
    </p>
    <ul>
        <li>Перед запуском: отфильтровать все дуги с весом = ∞.</li>
        <li>
            Если после фильтрации путь до узла назначения не существует —
            узел считается недостижимым, маршрут не строится.
        </li>
        <li>
            В отчёте отображать список недостижимых узлов и список
            отфильтрованных (недоступных) каналов отдельно.
        </li>
    </ul>
</div>

<!-- ═══════════════════════════════════════════════════════════
     БЛОК 5 — Пример маршрутизации
     ═══════════════════════════════════════════════════════════ -->
<div class="theory-section">
    <h3>Пример маршрутизации</h3>
    <p>
        Сеть из 5 узлов: A (источник), B, C, D, E.<br>
        Каналы: A→B:4, A→C:2, C→B:1, B→D:5, C→D:8, D→E:2, A→E:∞ (недоступен).
    </p>
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Узел назначения</th>
                <th>Минимальная задержка</th>
                <th>Маршрут</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>B</td>
                <td>3</td>
                <td>A → C → B</td>
            </tr>
            <tr>
                <td>C</td>
                <td>2</td>
                <td>A → C</td>
            </tr>
            <tr>
                <td>D</td>
                <td>8</td>
                <td>A → C → B → D</td>
            </tr>
            <tr>
                <td>E</td>
                <td>10</td>
                <td>A → C → B → D → E</td>
            </tr>
        </tbody>
    </table>
    <p>
        Канал A→E недоступен (∞) — исключён из расчёта.
        Узел E достижим обходным маршрутом через D.
    </p>
</div>

<!-- ═══════════════════════════════════════════════════════════
     БЛОК 6 — Переход к практике
     ═══════════════════════════════════════════════════════════ -->
<div class="btn-practice-wrap">
    <a href="/dijkstra/practice" class="btn-practice">Перейти к практике →</a>
</div>
